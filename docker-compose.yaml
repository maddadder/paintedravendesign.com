version: '2'

services:

  paintedravendesign:
      image: 192.168.1.84:32000/paintedravendesign:1.0.1
      build:
          context: ./
          dockerfile: ./Dockerfile
      restart: unless-stopped
      ports:
          - "5001:443"
